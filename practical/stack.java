private class DoubleyLinkedBase{
    // creating a another helper class.
    private class Node{
        private int element;
        private Node prev;
        private Node next;

        void Node(int element, Node prev, Node next){
            element = element
            prev = prev
            next = next
        }
    }
    private Node header = this.Node;
    private Node trailer;
    private int size = 0 ;

    void DoubleyLinkedBase(Node header, Node trailer, int size){
        header = header
        trailer = trailer
        size = size
    }

}